#include "../../Convert.h"
#include "$name$GraphWindow.h"

Graph::Settings $name$GraphWindow::settings = Graph::Settings($horizontal_autoscale$);

$name$GraphWindow::$name$GraphWindow(const juce::ValueTree& windowLayout, const juce::Identifier& type, DevicePanel& devicePanel_, GLRenderer& glRenderer)
        : GraphWindow(windowLayout, type, devicePanel_, glRenderer, "$y_axis$", $legend$, settings)
{
$callback_implementations$
}

$name$GraphWindow::~$name$GraphWindow()
{
    for (const auto callbackID : callbackIDs)
    {
        devicePanel.getConnection().removeCallback(callbackID);
    }
}
